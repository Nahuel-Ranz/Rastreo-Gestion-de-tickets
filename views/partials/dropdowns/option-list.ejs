<!-- list_id, data: [ "string1", ... ] -->

<ul id="<%= list_id %>" class="dropdownList flexColumn hidden">
    <% data.forEach(option => { %>
        <li><p><%= option %></p></li>
    <% }); %>
</ul>
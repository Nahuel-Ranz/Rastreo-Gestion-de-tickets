<div id="modal_container" class="modal_container flexColumn hidden">
    <div id="modal" class="modal flexColumn">
        <i id="close-modal" class="fas fa-times-circle"></i>
        <header class="flexRow hp">
            <img src="/media/rgt-white.png" alt="UTN-LOGO" id="logo" >
            <h2>Título del modal</h2>
        </header>
        <div id="modal-body" class="flexColumn hp vp">
            body
        </div>
        <footer class="flexRow hp">
            <img src="/media/merge-white.png" alt="UTN" />
            <img src="/media/seu-white.png" alt="SEU" />
        </footer>
    </div>
</div>

<script type="module">
    import { showElement, hideElement } from '/js/utils.js';
    
    ((d) => {
        const closer = d.getElementById('close-modal');
        const container = d.getElementById('modal_container');

        closer.addEventListener('click', () => hideElement(container));
        d.addEventListener('keydown', (e) => {
            if(e.key === 'Escape') hideElement(container);
        })
    })(document);
</script>
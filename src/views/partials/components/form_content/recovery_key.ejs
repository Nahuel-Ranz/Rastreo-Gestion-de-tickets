<!-- {
    group: { id:'', input:''},
    input: { id:'', type:'', req:},
    label: { id:'', content:''},
    validation: { regex, error_message }
} -->

<p>Se ha enviado a <strong>ranznahue@gmail.com</strong> una clave de recuperación. Copiela aquí debajo para continuar:</p>

<%- include('components/input_group', {
    group: { id:'credential_group', input:'credential_input'},
    input: { id:'credential', type:'text', req:true},
    label: { id:'credential_label', content:'Código de recuperación'},
    validation: {
        regex:/^\s*(?:[\w.%+-]+@(?:[a-z0-9-]+\.)+[a-z]{2,63}|\d{5,10})\s*$/i,
        error_message: 'Dato erróneo o incompleto.'
    }
}); %>

<div id="button_group" class="flexRow vp">
    <%- include('components/button', {
        btn: { id:'re-send_code', type:'', classes:''},
        content: { name:'Reenviar código', icon:''}
    }); %>

    <%- include('components/button', {
        btn: { id:'confirm', type:'submit', classes:''},
        content: { name:'Confirmar', icon:''}
    }); %>
</div>